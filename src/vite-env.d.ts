// ABOUTME: Triple-slash reference so TypeScript knows about Vite's injected import.meta.env types.
// ABOUTME: Required because tsconfig sets `types: []`, which excludes auto-included global types.
/// <reference types="vite/client" />
